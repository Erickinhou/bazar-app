import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flexDirection: "row",
        width: "92.5%",
        alignItems: "center",
        justifyContent: "center",
        padding: 10,
    },
    textInput: {
        minWidth: "90%",
        borderBottomWidth: 1,
    }
});